import React from 'react'
import { connect } from 'react-redux';
import PropTypes from 'prop-types'
import { getBooks } from '../../actions/book';


const BookList = ({ getBooks, book: { books, loading } }) => {

}